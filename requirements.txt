# === Vercel Production ===
fastapi>=0.115.0
requests>=2.32.0
numpy>=1.26.0
networkx>=3.0
click>=8.1.7
urllib3>=2.0
scipy>=1.11.0
osmnx>=1.9.0
geopandas>=1.0.0
shapely>=2.0.0
pyproj>=3.6.0

# === Optional / Local Dev ===
# torch>=2.4.0         # Heavy (~2GB) - only needed for NN training/inference
# torchdiffeq>=0.2.4   # Only needed for NN training
# matplotlib>=3.8.0    # Only for chart generation
# scikit-learn>=1.3    # Only for symbolic regression
# pytest>=8.3.0        # Only for testing
# uvicorn>=0.30.0      # Dev server (Vercel uses its own)