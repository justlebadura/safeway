[project]
name = "safeway-api"
version = "0.1.0"
description = "Backend de SafeWay con fórmulas destiladas"
requires-python = ">=3.10"
dependencies = [
    # Deja esto vacío si tus dependencias están en un requirements.txt, 
    # o lístalas aquí si no usas requirements.txt:
    "Flask>=3.0.0", 
]

[tool.vercel]
entrypoint = "backend.api:app"